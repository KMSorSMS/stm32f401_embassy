{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cargo",
            "command": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "args": [
                // "--release"
            ],
            "group": "build",
            "label": "build stm32f401re debug",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
        {
            "type": "cargo",
            "command": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "args": [
                "--release"
            ],
            "group": "build",
            "label": "build stm32f401re release",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ]
}